[mgr]
ratio = [3, 4, 5]
sort_by = "alphabetical"
sort_sensitive = false
sort_reverse = false
sort_dir_first = true
sort_translit = false
linemode = "size"
show_hidden = false
show_symlink = true
scrolloff = 5
mouse_events = ["click", "scroll"]

[preview]
wrap = "no"
tab_size = 2
max_width = 700
max_height = 900
cache_dir = ""
image_delay = 5
image_filter = "catmull-rom"
image_quality = 85
ueberzug_scale = 1
ueberzug_offset = [0, 0, 0, 0]

[tasks]
micro_workers = 10
macro_workers = 10
bizarre_retry = 3
image_alloc = 536870912  # 512MB
image_bound = [0, 0]
suppress_preload = false

# Yazi 25.12+ dropped the `"$@"` convention: file paths arrive through `%s`
# (all selected) / `%s1` (first). With `"$@"` the shell saw no arguments at
# all, so every opener here silently ran against nothing.
[opener]
# Send the file to the machine you are connected FROM (see container/open).
# block = true so the "no listener" hint is visible; the pause uses POSIX
# `read` because yazi runs openers with sh (dash), not bash.
open = [
  { run = '/usr/local/bin/open %s || { printf "\nPress Enter to continue..."; read _; }', desc = "Open on my machine", block = true, for = "unix" },
]
edit = [
  { run = '${EDITOR:=nvim} %s', desc = "$EDITOR", block = true, for = "unix" },
]
info = [
  { run = 'mediainfo %s | less -R', desc = "Media info", block = true, for = "unix" },
]
exif = [
  { run = 'exiftool %s | less -R', desc = "EXIF metadata", block = true, for = "unix" },
]
extract = [
  { run = 'ya pub extract --list %s', desc = "Extract here", for = "unix" },
]

# The first entry of each `use` is what <Enter> runs; the rest show up under
# `O` (open interactively). `reveal` is deliberately absent: it means "show in
# a file manager", which does not exist in a headless container.
[open]
rules = [
  { url = "*/", use = [
    "edit",
  ] },
  { mime = "text/*", use = [
    "edit",
    "open",
  ] },
  { mime = "application/pdf", use = [
    "open",
    "edit",
  ] },
  { mime = "image/*", use = [
    "open",
    "exif",
    "edit",
  ] },
  # Media defaults to metadata, not transfer: whole videos would have to cross
  # the tunnel, and the listener caps payloads at 512MB.
  { mime = "{audio,video}/*", use = [
    "info",
    "open",
  ] },
  { mime = "application/{,g}zip", use = [
    "extract",
  ] },
  { mime = "application/x-{tar,bzip*,7z-compressed,xz,rar}", use = [
    "extract",
  ] },
  { mime = "application/{json,x-ndjson}", use = [
    "edit",
    "open",
  ] },
  { mime = "*/javascript", use = [
    "edit",
    "open",
  ] },
  { mime = "inode/x-empty", use = [
    "edit",
  ] },
  { url = "*", use = [
    "open",
    "edit",
  ] },
]

[plugin]
prepend_previewers = [{ url = "*.md", run = "glow" }]

[[plugin.prepend_fetchers]]
id = "git"
group = "git"
url = "*"
run = "git"

[[plugin.prepend_fetchers]]
id = "git"
group = "git"
url = "*/"
run = "git"

[input]
cursor_blink = false
# cd
cd_title = "Change directory:"
cd_origin = "top-center"
cd_offset = [0, 2, 50, 3]
# create
create_title = ["Create:", "Create (dir):"]
create_origin = "top-center"
create_offset = [0, 2, 50, 3]
# rename
rename_title = "Rename:"
rename_origin = "hovered"
rename_offset = [0, 1, 50, 3]
# filter
filter_title = "Filter:"
filter_origin = "top-center"
filter_offset = [0, 2, 50, 3]
# find
find_title = ["Find next:", "Find previous:"]
find_origin = "top-center"
find_offset = [0, 2, 50, 3]
# search
search_title = "Search via {n}:"
search_origin = "top-center"
search_offset = [0, 2, 50, 3]
# shell
shell_title = ["Shell:", "Shell (block):"]
shell_origin = "top-center"
shell_offset = [0, 2, 50, 3]

[confirm]
# trash
trash_title = "Trash {n} selected file{s}?"
trash_origin = "center"
trash_offset = [0, 0, 70, 20]
# delete
delete_title = "Permanently delete {n} selected file{s}?"
delete_origin = "center"
delete_offset = [0, 0, 70, 20]
# overwrite
overwrite_title = "Overwrite file?"
overwrite_content = "Will overwrite the following file:"
overwrite_origin = "center"
overwrite_offset = [0, 0, 50, 15]
# quit
quit_title = "Quit?"
quit_content = "The following task is still running, are you sure you want to quit?"
quit_origin = "center"
quit_offset = [0, 0, 50, 15]

[pick]
open_title = "Open with:"
open_origin = "hovered"
open_offset = [0, 1, 50, 7]

[which]
sort_by = "none"
sort_sensitive = false
sort_reverse = false
sort_translit = false
